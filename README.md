# airbnb-project
Creating a Java Springboot application which can use AI features to create applications automatically. This will also have an UI with React framework for User interaction and will be a replication of AIRBNB
