spring:
  application:
    name: airBnbApp

# DB Connection Properties - POSTGRES
  datasource:
    url: jdbc:postgresql://localhost:9000/airbnb
    username: piyush
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

